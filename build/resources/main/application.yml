spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://svc.sel3.cloudtype.app:30875/imsDB
    username: 'root'
    password: '1234'
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    logging:
      level:
        org:
          hibernate:
            type:
              descriptor:
                sql: trace
jwt:
  secret: YOUR_SECRET_KEY
  access-expired: 86400
